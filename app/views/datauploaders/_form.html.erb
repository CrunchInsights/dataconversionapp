<%= form_for(@datauploader) do |f| %>
  <% if @datauploader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@datauploader.errors.count, "error") %> prohibited this datauploader from being saved:</h2>

      <ul>
      <% @datauploader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
